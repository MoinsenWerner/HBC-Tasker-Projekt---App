name: hbc_musik_client
description: Native Flutter port of the HBC Tasker music client.
publish_to: none
version: 4.0.7+407

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  credential_manager: ^5.1.0
  http: ^1.5.0
  shared_preferences: ^2.5.3
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
